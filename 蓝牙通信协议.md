# 蓝牙通信协议

## 1. 通信参数

| 项目 | 参数 |
|---|---|
| MCU串口 | UART1（`TRACE_UART`） |
| MCU引脚 | PA8 TX、PA9 RX |
| 波特率 | 115200 bit/s |
| 数据位 | 8 bit |
| 停止位 | 1 bit |
| 校验位 | 无 |
| 流控制 | 无 |
| 数据编码 | ASCII |

通信采用轮询接收，不使用UART1接收中断。

## 2. 竞赛题目与通信行为

| 题号 | 底盘行为 | 是否发送蓝牙帧 |
|---|---|---|
| Q1 | 图传项目，底盘不动作 | 否 |
| Q2 | 使用M1，一圈后定点停车 | 否 |
| Q3 | 底盘不动作，启动摆球控制装置并本地计时 | 是，持续发送`3\r\n` |
| Q4 | 使用M2，从A点行驶至B点 | 是，持续发送`4\r\n` |
| Q5 | 使用M3，整圈行驶并通过A点 | 是，持续发送`5\r\n` |
| Q6 | 使用M3，整圈行驶，摆球保持指定位置 | 是，持续发送`6\r\n` |

## 3. 下行数据帧（底盘发送给摆球控制器）

### 3.1 题号帧

Q3～Q6使用固定3字节ASCII帧：

```text
<题号>\r\n
```

| 题目 | ASCII内容 | 十六进制字节 |
|---|---|---|
| Q3 | `3\r\n` | `33 0D 0A` |
| Q4 | `4\r\n` | `34 0D 0A` |
| Q5 | `5\r\n` | `35 0D 0A` |
| Q6 | `6\r\n` | `36 0D 0A` |

发送周期为100 ms，即10 Hz。

### 3.2 开始发送的条件

满足以下任一条件时开始持续发送当前题号：

1. 在OLED上点击`PREP`；
2. 直接点击`RUN`。

`PREP`不是发车的必要条件，未收到OK也允许点击RUN。

### 3.3 持续发送规则

- 点击RUN后不会停止发送题号帧；
- 底盘启动、运行、停车以及Q3计时结束后仍持续发送；
- 当前协议不再发送`go3`、`go4`、`go5`或`go6`；
- 切换题号会停止旧题号的发送；
- 在发车前的`WAIT/OK`状态再次点击PREP，可以取消准备并停止发送；
- 系统复位后停止发送，默认选择Q2。

## 4. 上行数据帧（摆球控制器回复底盘）

摆球控制器可回复：

```text
ok3
ok4
ok5
ok6
```

接收规则：

- 大小写不敏感，例如`OK3`、`Ok3`和`ok3`均有效；
- 回复可以带`\r`、`\n`或`\r\n`，也可以完全不带换行；
- 仅匹配当前所选题号，例如Q4状态下`ok3`不会被确认；
- OK只用于OLED显示对面已准备，不作为发车许可；
- 即使始终没有收到OK，底盘也允许发车。

推荐回复格式：

```text
ok3\r\n
```

## 5. 操作时序

### 5.1 使用PREP确认

```text
底盘选择Q4
    ↓
点击PREP
    ↓
底盘每100 ms发送 4\r\n
    ↓
摆球控制器回复 ok4\r\n
    ↓
OLED显示 PREP:OK4
    ↓
点击RUN
    ↓
底盘继续发送 4\r\n，并在20 ms后启动M2
```

### 5.2 不等待OK直接启动

```text
底盘选择Q5
    ↓
直接点击RUN
    ↓
底盘开始每100 ms发送 5\r\n
    ↓
20 ms后启动M3
```

## 6. Q3本地计时

- 点击Q3的RUN时开始记录本地时间；
- Q3只启动摆球控制装置，底盘电机不动作；
- 第二次点击RUN可以提前停止计时；
- 未手动停止时，计时到5.00 s自动结束；
- OLED显示格式为`TIME:x.xx s`；
- 计时结束后仍继续发送`3\r\n`。

Q3计时是底盘本地计时，不依赖摆球控制器发送完成帧。

## 7. 协议限制

- 当前帧没有校验和；
- 当前帧没有单独的帧头，依靠固定单字符题号和`\r\n`分帧；
- Q1、Q2不发送题号帧；
- 当前协议没有停止命令和完成命令；
- 串口不再发送VOFA浮点数据和比赛运行LOG，避免干扰题号帧。

